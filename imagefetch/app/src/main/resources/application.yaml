logging:
  level:
    com:
      bla:
        imagefetch:
          app: info
spring:
  profiles:
    active: commonDal, commonUtil, coreService

imageDirectory: E:\imageFactory\images
imageTaskCron: 0/5 * * * * ? *
server:
  port: 8081
